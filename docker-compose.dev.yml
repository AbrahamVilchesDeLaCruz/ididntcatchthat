# Dev override — local development
# Variables injected by Doppler (config: dev) via Makefile
services:
  api:
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: ${NODE_ENV}
      PORT: ${PORT}

  client:
    ports:
      - "4001:80"
    environment:
      NODE_ENV: ${NODE_ENV}
